# Semantic Git & ZK-Compiler Integration
(Phase 27 V1.0 Upgrade)

- Replaced standard diff view with 3D Lineage Tree.
- `explore/fallback` AST nodes are now syntactically highlighted.
- Auto-compiles `.0` files into ZK-Circuits on Save.
